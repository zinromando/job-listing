<h1> Edit a job </h1>

<%= simple_form_for [:admin, @job ] do |f| %>

<%= f.input :title, :label => "职位" %>
<%= f.input :description, :label => "工作内容"%>
<%= f.input :wage_lower_bound, :label => "薪资下限" %>
<%= f.input :wage_upper_bound, :label => "薪资上限" %>
<%= f.input :contact_email, :label => "联系邮件" %>

  <%= f.submit "Submit" %>
<% end %>
